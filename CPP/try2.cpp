#include <iostream>
#include <vector>
using namespace std;

int main()
{int i, f[3]={};
  vector <int> *a;
  (*a)[i] = f[0];
  for (i=0; i<3; i++)
    {(*a).push_back(i);
      cout << f[i] << '\n';
    }
}
